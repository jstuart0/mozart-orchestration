## D1 — pin the runtime image tag (2026-09-17T00:00Z, stage 3-plan)
- **Reasoning**: the floating tag broke a build last time
- **Bounds accepted**: not re-testing the floating tag this campaign
- **Revisit when**: harmless while the pin holds; revisit when upstream cuts a new stable
