## D1 — keep the single-column layout over ruby's two-column proposal (2026-09-01T00:10Z, stage 4-plan-review)
- **Reasoning**: the two-column layout regresses at 320px, and no consumer requested it
- **Bounds accepted**: revisiting this if a consumer asks for the wide layout
- **Revisit trigger**: harmless while no consumer needs the wide layout; revisit when one does
